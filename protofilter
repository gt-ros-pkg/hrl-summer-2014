# Prototype filter for torque sensor data - HRL Summer Project - June 2, 2014

# One issue with this file is that it only detects a constant task
# In other words, functionality to differentiate between different portions (stages)
# of the task need to be included. This could either be done in this file, or int
# a handling (higher-level) file. It should also be noted that the numbers in 
# the example file may require adjustments to the filter values.
def check_data(values,training)
  ma1, ma2, ma3 = -sys.maxint -1 #System values.
  mi1, mi2, mi3 = sys.maxint
  if training == True :
     if values[0] > ma1 :
         ma1 = values[0]
     elif values[1] > ma2 :
         ma2 = values[1]
     elif values[2] > ma3 :
         ma3 = values[2]
     elif values[0] < mi1 :
         mi1 = values[0]
     elif values[1] < mi2 :
         mi2 = values[1]
     elif values[2] < mi3 :
         mi3 = values[2]
     return True
  else :
      bma1 = ma1 * 1.10
      bma2 = ma2 * 1.10
      bma3 = ma3 * 1.10
      bmi1 = mi1 * 0.90
      bmi2 = mi2 * 0.90
      bmi3 = mi3 * 0.90
      if values[0] > bma1 or values[1] > bma2 or values[2] > bma3 or values[0] < bmi1 or values[1] < bmi2 or values[2] < bmi3 :
         return False
      else :
         return True
